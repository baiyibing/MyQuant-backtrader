# cursor-desktop 评审（host 空槽）

> 待评审：`docs/backtest/plan-unify-csv-strategies-1-8-2026-09-12.md` **v1.1**
> 状态：classic 四家 rc=0；已综合为 `merge-consensus.md`；plan **v1.2** 可进切片 A。本槽不计独立票。

## 主持裁已锁（对抗层，不计票）

v1 按原文不可实施。v1.1 吸收：None 止损短接、3/5 引擎时钟、reason 前缀、日线 5 不做 force、SMA 交易日预载、池函数禁回落、市场层叶子、实施序先引擎后 register。

未让步：1–8 都接到向量化；7 不进 BOOKS；不改 presets；化石仅 E；本轮不改北交档 / 停牌追买。

## 请四家重点核（避免重复对抗已锁项）

1. v1.1 的 U-R8（日线 5 无 14:50）与切片 B′ 是否自洽。
2. U-R19 当日收盘开板会不会改到 6/8 pending。
3. U-R24 空文件分叉是否够护 7 的指数日历。
4. `scan_held_day` 扩签名 vs 独立分支：6/8 回归是否可测。
5. 是否还有新的 🔴（事实互斥），而不是把已吸收的对抗条再打一遍。

综合见完成后的 `merge-consensus.md`。
