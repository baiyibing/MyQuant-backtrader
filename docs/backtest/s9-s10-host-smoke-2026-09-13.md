# 策略 9 / 10 宿主烟测（2026-09-13）

> **落盘**：2026-09-13。
> **计划**： [plan-strategy9-bottom-vol-2026-09-13.md](plan-strategy9-bottom-vol-2026-09-13.md) · [plan-strategy10-tr-pool-2026-09-13.md](plan-strategy10-tr-pool-2026-09-13.md) · [plan-source-b-ta-pool-2026-09-13.md](plan-source-b-ta-pool-2026-09-13.md)。
> **对照**：M5 二轮三源 [m5-list-attribution-2026-03-09.md](m5-list-attribution-2026-03-09.md)。本文**不是**第四列净值赛，不改 topk，不重开 `--asof`。
> **产物**：`exports/`、`backtest_output/`（gitignore）。活 Store 未写回。

---

## 0. 一句话

9 能在真湖上稳定写出契约日 CSV，且与 pred / 手工几乎不是同一天同一拨人。10 的规则能出票，但活 Store 没有 TR 布林带；默认湖宇宙遇到「湖有、Store 无」会 fail-closed。两份名单都不当冠军。

---

## 1. 策略 9（底量超顶量）

窗 `20260303`–`20260908`，主板+中小 3206 只，`export_strategy9_pool.py`。

| 项 | 数 |
|----|----|
| 文件 | **131**（与 pred 日历对齐，差集空） |
| 名额 / 不重复 | 3484 / **382** |
| `validate_pool_dir` | `[]` |
| 空日 | **0**（相对 pred 131 日） |
| 宽度 | 1–62；月均 3 月 21.3、4–7 月 28–37、**8 月 5.2**、9 月前 6 日 8.2 |
| 相对手工快照缺日 | `20260525`、`20260605`（手工本来没有，不当管道失败） |

同日交集（对齐 pred 的 131 日；手工 / 手工 Top10 各 129 日）：

| 对照 | 同日有交集的天数 | 跨日并集 |
|------|------------------|----------|
| pred Top10 | **1**（`20260624` 的 `600615`） | 18 |
| 手工冻结 | **0** | 0 |
| 手工 Top10 | **0** | 27 |

短窗 `20260303`–`20260313` 先跑通：9/9 日、135 名额、36 只；同日 ∩ pred / 手工 / 手工 Top10 全空。`csv_daily --strategy version9` 能吃名单（短窗买入 36；20 日强平未到）。净值不写入结论。

---

## 2. 策略 10（源 B / `resist_tr_bb_1000`）

活 Store：`F:\stock_data\turnover_resistance_daily.parquet`（约 149MB，313 万行，`window=1000`）。

| 项 | 数 |
|----|----|
| 覆盖 | `20240102`–`20260604` |
| `tr_bb_*` / `bands_computed_at` | **全空** |
| `20260303` 原始截面 | 5471 |
| `require_bands=True` | **0**（活文件） |
| `20260605` / `20260908` | 无行（缺日） |

官方短窗 `export_ta_pool.py --start 20260303 --end 20260306` 在活 Store 上第一天即：

`ConfigurationError: TR filter fail-closed: empty cross_section for 20260303`

这是设计，不是静默用 Store 成员冒充宇宙。

带只在**副本**上算过，未写回 F:：`exports/s10_store_copy_20251201_20260313.parquet`（切片 365,589 行，更新 261,634；`20260303` 有带 5460 只）。

默认湖宇宙 + 带副本：湖当日有 K 5471，截面 5460，缺行 21 只（含 `001220.SZ`）→

`TR filter fail-closed: missing cross_section row for '001220.SZ'`

`--universe-file` = 短窗 9 日截面交集（5460 只）后写出：

| 项 | 数 |
|----|----|
| 窗 | `20260303`–`20260313` |
| 文件 / 名额 / 不重复 | 9 / 138 / **113** |
| `validate_pool_dir` | `[]` |
| 空日 | 0/9 |
| 每日宽度 | 2, 2, 15, 57, 1, 31, 15, 9, 6 |
| 同日 ∩ pred / 手工 / 手工 Top10 | **0 / 0 / 0** |
| 同日 ∩ 策略 9 | 4 天、5 只（`002461`、`600177`、`601528`、`601728`、`601998`） |

`csv_daily --strategy version10` 能吃这份名单（买入 112）。净值不写入结论。

---

## 3. 结论（锁）

1. **9 是第三套人。** 长窗同日几乎不碰 pred / 手工；跨日并集有少量同票，从不同日。不是 M5 第四列。
2. **10 能出票，但不能当日常默认源。** 活 Store 缺带；默认湖宇宙缺行会中断整天。补带是 TR 生产管道，会写 F:，且 Store 只到 `20260604`，盖不住 M5 后半窗。
3. **fail-closed 保持。** 不改成「缺行跳过」。不改 topk，不加 80 窗，不把 9/10 净值跟 pred 赛跑。
4. **L2 / trade_decision 仍后置。** 晋升链没到。

---

## 4. 命令（已跑；产物不入库）

```text
D:\anaconda3\envs\vanna312\python.exe scripts/data/export_strategy9_pool.py --start 20260303 --end 20260908 --out-dir exports/s9_bvot_20260303_20260908

D:\anaconda3\envs\vanna312\python.exe scripts/data/export_ta_pool.py --start 20260303 --end 20260313 --store-parquet exports/s10_store_copy_20251201_20260313.parquet --universe-file exports/s10_smoke_universe_store_inter.txt --out-dir exports/src_b_tr_bb1000_20260303_20260313

D:\anaconda3\envs\vanna312\python.exe backtest/research/csv_daily_backtest.py --strategy version9 --pool-dir exports/s9_bvot_20260303_20260313 --start 20260303 --end 20260313 --out-dir backtest_output/s9_smoke_20260303_20260313
D:\anaconda3\envs\vanna312\python.exe backtest/research/csv_daily_backtest.py --strategy version10 --pool-dir exports/src_b_tr_bb1000_20260303_20260313 --start 20260303 --end 20260313 --out-dir backtest_output/s10_smoke_20260303_20260313
```
