# Plan：M5 二轮（长窗三源归因；本仓任务 3）

> **落盘**：2026-09-13。
> **状态**：📄 **v1.0 · A–D 本机已跑**。报告 [m5-list-attribution-2026-03-09.md](m5-list-attribution-2026-03-09.md)。结论：**长窗仍几乎不重叠**（129 对齐日 pred∩hand / pred∩hand10 全空）。
> **上游**：MyQuant `docs/plan-m5-round2-2026-09-13.md`（任务 3 交接规格）。中期同步见 [myquant-progress-sync-2026-09-13.md](myquant-progress-sync-2026-09-13.md)。
> **前序**：首轮 [plan-m5-list-attribution-2026-09-13.md](plan-m5-list-attribution-2026-09-13.md) / [m5-list-attribution-2026-03.md](m5-list-attribution-2026-03.md)。工具已合 [#27](https://github.com/baiyibing/MyQuant-backtrader/pull/27)。
> **风险档**：**L1**（冻结快照 + 对照跑 + 报告；不重写引擎、不改卖点、不改 `--asof`、不用 PortAna）。

---

## 0. 一句话

同一本 `version6` 书、同一资金、同一买入日窗 `20260303`–`20260908`，只换名单：Qlib 扩展窗 Top10 / 手工原样 / 手工 Top10。回答「长窗下三源是否仍几乎不重叠」，**不**据此改 topk 或卖点。

```text
MyQuant 预测结果_ext.csv          （任务 1，本仓只读；master 尚无脚本）
        ↓  export --asof pred_minus_one --topk 10
MyQuant/exports/m5r2_pred_topn10_20260302_20260908/
        ↓  本仓直读或拷到 exports/m5r2_pred_topn10_*
csv_daily --strategy version6 --out-dir <pred|hand|hand10>
```

---

## 1. 现锁（M5R2-*）

| ID | 锁 |
|----|----|
| **M5R2-1** | 三列缺一不算完成：① 扩展窗 pred Top10；② 冻结后的手工原样；③ 同一冻结目录截断 Top10。禁止只用现网 `stock_pool/` 当 ②（会续写）。 |
| **M5R2-2** | 书 = `version6`；窗 = **pred 目录实际存在的首末文件名**（H0 预期 `20260303`–`20260908`）。三列同一 `--start/--end`、同一 `--cash-total` / `--daily-quota`。`version8` 可选、另 `--out-dir`。不喂 7。日线。 |
| **M5R2-3** | pred 必须来自 MyQuant 任务 1/2 的 `预测结果_ext.csv` 导出，目录名带 `m5r2`。禁止首轮 `exports/r2_pred_topn_r3_*`、禁止 `exports/r0_*`。`--asof pred_minus_one`、`--topk 10`。**不加 topk=5 臂**（那是 M3-B；OOS 已证明三月冠军出窗垫底）。 |
| **M5R2-4** | 手工：先把 `stock_pool/YYYYMMDD.csv` 在 `[start,end]` 拷到 `exports/m5r2_hand_snap_{start}_{end}/`（gitignore），再对快照跑 `m5_hand_topn.py`。不写 `stock_pool/`。本机 2026-09-13 该窗有 **129** 个手工日（`20260303`–`20260908`）；缺日写入报告，不当管道失败。 |
| **M5R2-5** | 报告只写：本仓三件套（全资金 / 动用 / 最大回撤 / 买卖桶 / 涨停跳过 / 加载代码 / 池天数）+ 逐日 pred∩hand、pred∩hand10 + 日均重叠率 + 名单换手（当日相对昨日新增比例）。禁止 PortAna IR。禁止「模型优于手工」。 |
| **M5R2-6** | 结论允许：长窗仍几乎不重叠 / 重叠随月份变化 / 宽度主导 / 宽度对齐后仍分不出。重叠度随窗长变化**本身是结论**。禁止改 `--asof`、禁止用单窗 NAV 改参。 |

---

## 2. 非目标

| 不做 | 原因 |
|------|------|
| 写 `predict_extended.py` / 重训 | 住 MyQuant #18 任务 1 |
| 改 `execute_buy` / `_sell` / `presets.py` / 策略 7 | 归因轮 |
| 本仓 `import qlib` | 只消费 CSV |
| 把 M3-B `topk5` 写进名单 | 任务书与 OOS 预锁 |
| 随机名单第四列当完成定义 | 可后置；本轮三源即可 |
| 合入 `exports/` / `backtest_output/` / 真 pred | gitignore |
| 等 M3-D 中性化 | 排序层，不是本仓成交 |

---

## 3. 切片

从当前 master 开 `feat/m5-round2`（本 docs 分支只写计划）。不要在本分支改成交核。

| 切片 | 做什么 | 完成定义 | 依赖 |
|------|--------|----------|------|
| **A · 冻结手工快照** | `scripts/data/m5_freeze_hand_pool.py`：按窗拷贝 `stock_pool` → `exports/m5r2_hand_snap_*`；字节原样；拒绝写回 `stock_pool/` | 夹具 2 个文件拷对；缺日计数打印 | 无 |
| **B · 等 pred** | 核对 MyQuant 导出目录：`validate_pool_dir=[]`；天数 = pred 日 − 1 | 记下首末 stem | MyQuant 任务 1/2 |
| **C · 三列实跑** | pred / snap / hand10 各一趟 `version6`，显式 `--out-dir` | 三份 `summary.txt` | A + B |
| **D · 报告** | `docs/backtest/m5-list-attribution-2026-03-09.md` | 填 M5R2-5 / M5R2-6；链本文 | C |

A 可在 pred 到达前做。C 不提交数字文件。

```text
D:\anaconda3\envs\vanna312\python.exe -m pytest -q tests/test_m5_freeze_hand_pool.py tests/test_m5_hand_topn.py
```

---

## 4. 代码落点（实施分支，不在本 PR）

| 文件 | 动作 |
|------|------|
| `scripts/data/m5_freeze_hand_pool.py` | **新建** A |
| `tests/test_m5_freeze_hand_pool.py` | A |
| `docs/backtest/m5-list-attribution-2026-03-09.md` | **新建** D |
| `docs/backtest/README.md` | D：SSOT 一行 |

复用：`csv_daily --out-dir`、`m5_hand_topn.py`、`validate_pool_dir`、`parse_pool_csv`。

禁止改：`csv_ledger.py`、`presets.py`、`csv_strategy_books.py`、v7、`--asof`、`docs/architecture/reviews/**`。

---

## 5. 风险

- 手工池不是 2026-03 当时冻结快照；二轮必须先拷再跑，报告写快照日期。
- 129 个手工日 vs ~128 个 pred 买入日：对齐用 **两边都有文件的日期**，缺的一边记缺口。
- 长窗日线约半年 × 三列，冷湖会慢；仍只用日线。
- MyQuant OOS 已证明单窗 sweep 会骗人；本轮 NAV 同样不能改参。

---

## 6. 修订程序

改 M5R2-* 须改本文。改买入日语义须改 [pool-csv-contract.md](pool-csv-contract.md)。首轮 M5-R* 不改，除非回写首轮报告错误。
