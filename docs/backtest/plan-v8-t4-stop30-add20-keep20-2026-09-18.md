# Plan：止盈保留/保底改档 + 止损 30% + 上一笔×1.2 加一笔

> **日期**：2026-09-18
> **状态**：✅ **已落地并已跑分钟 5 亿**。目录 `_v8_t4_stop30_last12_keep20`（500,842,378.74 / +0.17%）。不覆盖 `_stop10` / `_v8_3` / `_v8_livermore` / `_v8_3_hold5` / `_v8_3_hold5_stale8` / `_v8_stop10_t4` / `_v8_t4_stop30_add` / `_v8_t4_stop30_add20` / `_v8_t4_stop10_add20`。
> **风险档**：L1+（只换策略 8 止损、档位线、加仓锚；T+4 / 僵持 / 档位边界不动；不改 1–6/9/10，不改 E-R1–E-R6 成交核）
> **基线书**：现行 T4-stop10-add20，计划 [plan-v8-t4-stop10-add20-2026-09-18.md](plan-v8-t4-stop10-add20-2026-09-18.md)，产物 `_v8_t4_stop10_add20`（+1.33%）
> **落地时**：改 `STOP_PCT`、`KEEP_*`、档 7/8 保底、`may_add` 锚；档 2–6 保底数字不动。

---

## 0. 一句话

档 2–4 保留 20%；档 5–6 保留 40%；档 7/8 补保底并改保留；止损 30%；现价 ≥ **上一笔成交价 ×1.2** 再加一个基数。

---

## 1. 已确认（2026-09-18，原话）

| # | 确认人原话 | 锁定 |
|---|------------|------|
| 1 | 「档2，3，4：保留30*涨幅改为保留20%*涨幅」 | `KEEP_LOW=0.20`。档 2/3/4 仍 `max(现行保底, 买价+20%×涨幅)`。 |
| 2 | 「档5，6：保留50%*涨幅改为40%*涨幅」 | `KEEP_MID=0.40`。档 5/6 仍 `max(现行保底, 买价+40%×涨幅)`。 |
| 3 | 「档7：保底买价*1.35，买价+50%涨幅」 | 档 7 改为 `max(买价×1.35, 买价+50%×涨幅)`。 |
| 4 | 「档8：保底买价*1.6，买价+60%*涨幅」 | 档 8 改为 `max(买价×1.60, 买价+60%×涨幅)`。 |
| 5 | 「止损30%」 | `STOP_PCT=0.30`。仍无 giveback。T+1 起评。 |
| 6 | 「买价*1.2加仓一个基数」 | 已被第 7 条改口，作废。 |
| 7 | 「上一笔加仓价*1.2，加仓一个基数」 | 锚 = 该码**最近一笔未平成交价**（只有首笔时即首笔买价）。现价 ≥ 锚 ×1.2 才加；一次只加一个 100 万基数。 |

档位边界不动：死区 `(0,3%]`；档2 `(3%,6%)`；档3 `[6%,9%)`；档4 `[9%,15%)`；档5 `[15%,30%)`；档6 `[30%,50%)`；档7 `[50%,100%)`；档8 `≥100%`。

涨幅 = 峰值 − 买价。现价 ≤ 触发线则 `trail:band:N`。

加仓梯子跟**上一笔成交价**走，不是最早一笔、也不是固定 1.2 / 1.4 / 1.6。恰好 ×1.2 算够。未到 → `skip_add_loser`。

例（首笔买价 10）：

| 已有 | 下一笔门槛 | 含义 |
|------|------------|------|
| 1 笔 @10 | 12.00 | 首笔视为上一笔 |
| 2 笔，末笔 @12 | 14.40 | 12×1.2，不是 10×1.4 |
| 2 笔，末笔 @13 | 15.60 | 按实际加仓价再乘 |
| 3 笔，末笔 @14.40 | 17.28 | 可继续加，无笔数上限 |

上一笔卖掉后，锚改跟剩余里最近一笔。

---

## 2. 未写明（实施时按此，要改请先说）

| 项 | 实施默认 | 理由 |
|----|----------|------|
| 档 2–6 保底 | 仍 1.005 / 1.01 / 1.03 / 1.07 / 1.14 | 确认人只改保留比例 |
| T+4 / 死区 / 僵持 20 / 峰差 15 | 不动 | 未提 |
| 加仓何时评 | 仍只在**当日名单有该码**时评；同一天同一码最多加 1 笔 | 引擎原样；跳空越过两档也只加一笔 |
| 笔数上限 | **不设**，只受现金和名单 | 锚已改成上一笔×1.2，可连加 |
| 上证十日线 | 门控日新开和加仓都拦 | 引擎原样 |
| 名单 / 窗 / 现金 | `stock_pool/`，20251023–20260909，5 亿 | 近几轮相同 |
| 跌停 / T+0 / 佣金 / E-R6 | 引擎原样 | 不改成交核 |

---

## 3. 批准实施后的完整买卖点

```
止损 30%（T+1 起；无 giveback）
T+4 起评止盈：
        (0,3%]→不评档位
        (3%,6%)→max(买价×1.005, 买价+20%×涨幅)
        [6%,9%)→max(买价×1.01, 买价+20%×涨幅)
        [9%,15%)→max(买价×1.03, 买价+20%×涨幅)
        [15%,30%)→max(买价×1.07, 买价+40%×涨幅)
        [30%,50%)→max(买价×1.14, 买价+40%×涨幅)
        [50%,100%)→max(买价×1.35, 买价+50%×涨幅)
        [100%,∞)→max(买价×1.60, 买价+60%×涨幅)
满 20 日且峰值从未到 +6% → stale
已持加仓：现价 ≥ 最近一笔未平成交价 ×1.2，且当日名单有该码
        一次一个基数（100 万）；各 lot 独立评卖
上证十日线两日下方：当日新开与加仓都不做
```

`may_add`：无持仓 → True；否则 `px ≥ lots[-1].cost × 1.2`。

买价 10 时各档线（与现行单测同峰值）：

| 档 | 峰值 | 现行线 | 新线 |
|----|------|--------|------|
| 2 | 10.40 | 10.12 | **10.08** |
| 3 | 10.70 | 10.21 | **10.14** |
| 4 | 11.20 | 10.36 | **10.30** |
| 5 | 12.00 | 11.00 | **10.80** |
| 6 | 14.00 | 12.00 | **11.60** |
| 7 | 16.00 | 13.60（无保底） | **13.50** |
| 8 | 20.00 | 17.00（无保底） | **16.00** |

---

## 4. 明确不改

- 策略 1–6 / 9 / 10，成交核 E-R1–E-R6
- T+4、档位开闭区间、僵持 20、+6% 武装、峰差 15
- 档 2–6 保底数字
- 归档 `_stop10` / `_v8_3` / `_v8_livermore` / `_v8_3_hold5` / `_v8_3_hold5_stale8` / `_v8_stop10_t4` / `_v8_t4_stop30_add` / `_v8_t4_stop30_add20` / `_v8_t4_stop10_add20`：不覆盖
- Mode B 利弗莫尔冻结本 `livermore_exit_rules.py`
- 未再一句「批准实施」之前：不改 Python、不跑 5 亿

---

## 5. 批准实施后改哪些文件

| 文件 | 做什么 |
|------|--------|
| `backtest/research/strategy8_rules.py` | `STOP_PCT=0.30`；`KEEP_LOW=0.20`；`KEEP_MID=0.40`；`KEEP_50_100=0.50`；`KEEP_100=0.60`；`FLOOR_50_100=1.35`；`FLOOR_100=1.60`；档 7/8 走 `max`；`may_add` 锚改为 `lots[-1].cost×1.2`；HELP_LOCK / docstring / `record_strategy8_params` |
| `backtest/research/csv_artifacts.py` | summarize 改本包档位句 + 加仓上一笔×1.2 |
| `backtest/research/csv_strategy_books.py` | `--stop-pct` help 改回 v8 0.30 |
| `docs/backtest/README.md` | 策略 8 段改本包，链本 plan |
| `tests/test_strategy8_rules.py` 及 v8 daily/minute/hooks | 按下节向量翻 |

落地后跑分钟 5 亿，目录 **`_v8_t4_stop30_last12_keep20`**。

---

## 6. 单测向量（批准实施后要绿）

1. `n_days=1`、现价 −30% → 止损；−20% **不**止损。
2. T+3 破新线也不止盈；T+4 才评。
3. 档 2 峰值 10.40：线 10.08。
4. 档 3 峰值 10.70：线 10.14。
5. 档 4 峰值 11.20：线 10.30。
6. 档 5 峰值 12.00：线 10.80。
7. 档 6 峰值 14.00：线 11.60。
8. 档 7 峰值 16.00：线 13.50。
9. 档 8 峰值 20.00：线 16.00。
10. 创业板跳空：D1 −20% 未到 30% 止损；D2 贴跌停顺延；D3 `gap_open`（defer=1）。
11. 一笔成本 10、现价 11.99 → 不加；12.00 → 可加第 2 笔。
12. 已有 2 笔（末笔成本 12）、现价 14.00 → **不加**（12×1.2=14.40）。
13. 已有 2 笔（末笔成本 12）、现价 14.40 → 可加第 3 笔。

---

## 7. 落地

2026-09-18 确认人回复「批准实施」后已按上表改止损、档位线和加仓锚。分钟 5 亿退出码 0：`backtest_output/csv_minute_v8_20251023_20260909_v8_t4_stop30_last12_keep20/`。期末净值 500,842,378.74 / +0.17%。买入 2776、加仓 4、止损 53、回撤 2076、强制 604。未覆盖 `_stop10` / `_v8_3` / `_v8_livermore` / `_v8_3_hold5` / `_v8_3_hold5_stale8` / `_v8_stop10_t4` / `_v8_t4_stop30_add` / `_v8_t4_stop30_add20` / `_v8_t4_stop10_add20`。
