# Plan：止损 30% + 6/10/20 保底 + 峰差 30 + 僵持 30 + 涨停顺延

> **日期**：2026-09-19
> **状态**：✅ **已落地并已跑分钟 5 亿**。目录 `_v8_stop30_gap30_max110_max120_stale30_defer`（503,108,605.83 / +0.62%）。未覆盖 `_v8_stop6_gap30_floor102_stale30_defer` 及更早归档。
> **风险档**：L2（`STOP_PCT` + ≥20% 离场线；成交核不改）
> **基线书**：stop6-gap30-floor102-stale30-defer，产物 `_v8_stop6_gap30_floor102_stale30_defer`（499,153,546.43 / −0.17%）

---

## 0. 一句话

止损改为 30%。[6%, 10%) ×1.02 与 [10%, 20%) max(×1.10, 买价+50%涨幅) 沿用；新增 ≥20% max(×1.20, 买价+50%涨幅)。峰差 30、僵持 30、涨停顺延、只做首笔、上证下方仍开新仓不动。

---

## 1. 确认人原话

| 原话 | 锁定 |
|------|------|
| 止损改为30% | `STOP_PCT=0.30` |
| [6%, 10%) ×1.02 | `trail:band:6_10` |
| ≥10% = max(买价×1.10, 买价+50%×涨幅) | 峰值 `[10%, 20%)` → `trail:max110_50` |
| ≥20= max(买价×1.20, 买价+50%×涨幅) | 峰值 `≥20%` → `trail:max120_50` |
| 修改优化后执行 | 立即改书并跑分钟 5 亿 |

未再写的项沿用：`PEAK_GAP_MIN=30`，`STALE_DAYS=30`，`DEFER_LIMIT_UP=True`，`ALLOW_ADD=False`，`INDEX_GATE_ON=False`，无 4–6% 保底。

---

## 2. 落地目录

`_v8_stop30_gap30_max110_max120_stale30_defer`

勿覆盖 `_v8_stop6_gap30_floor102_stale30_defer` 及更早归档。

---

## 3. 落地

2026-09-19 确认人「修改优化后执行」后已换成上表。分钟 5 亿退出码 0：`backtest_output/csv_minute_v8_20251023_20260909_v8_stop30_gap30_max110_max120_stale30_defer/`。期末净值 **503,108,605.83 / +0.62%**，最大回撤 −8.16%。买入 3648、加仓 0、`skip_held` 920、止损 99、锚定回撤 2475、强制 1007、止盈 0、开板 0、`skip_cash` 0。相对 stop6 包（−0.17% / 止损 1847 / 强制 48）净值转正，止损几乎消失，僵持平仓暴增到 1007；回撤从 −3.95% 放到 −8.16%。未覆盖既有归档。
