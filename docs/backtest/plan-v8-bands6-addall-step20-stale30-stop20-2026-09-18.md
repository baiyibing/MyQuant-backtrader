# Plan：bands6 + 名单全加 + 独立 +20% + 僵持 30 + 止损 20%

> **日期**：2026-09-18
> **状态**：✅ **已落地并已跑分钟 5 亿**。目录 `_v8_bands6_addall_step20_stale30_stop20`（507,050,596.62 / +1.41%）。未覆盖 `_v8_stop10_bands6` / `_stop10` / `_v8_t4_list_step20_ind_stale30`。
> **风险档**：L2（加仓两通道 + 止损 / 僵持；不改六档数字、不改成交核）
> **基线书**：现行 stop10-bands6，产物 `_v8_stop10_bands6`（507,573,114.90 / +1.51%）
> **对照**：T+4 台阶包 `_v8_t4_list_step20_ind_stale30`（+1.20%，止损仍 10%）

---

## 0. 一句话

六档止盈不动。名单再现输家也加；相对第一笔成本每满 +20% 再加一个独立 100 万。僵持改 30 日。止损改 20%。

---

## 1. 已确认（2026-09-18，原话）

| # | 确认人原话 | 锁定 |
|---|------------|------|
| 1 | 「赢家输家都加」 | `may_add`：当日名单有已持、现价有效即加 **一个** 独立 100 万。现价 9.90 / 11.00 都加。取消「只加赢家」。 |
| 2 | 「每上涨20%加一个100万基数并单独计算」 | 锚 = 仍开着的 **lot 0** 成本。10→12→14→16。新 lot **独立**成本/峰值/`n_days`，自己评 20% 止损和六档。**不**设 `ride_with`。 |
| 3 | 「满 30 日且峰值从未到 +6%」 | `STALE_DAYS=30`。 |
| 4 | 「止损20%」 | `STOP_PCT=0.20`。T+1 起评。 |

---

## 2. 引擎两通道（与上一本台阶包同形）

| 通道 | 何时 | 结果 | 卖 |
|------|------|------|-----|
| 名单 | 当日名单有已持 | 新 lot，独立 | 自己评 20% / 六档 |
| +20% | 已持扫描；名单外也评 | 新 lot，`is_step=True`，`reason=add:step20` | 自己评 |

同一日两条都能开火：先名单、后最多一个台阶。跳空 +40% 当日只加 **一个** 台阶。`step_add_due` 只数 `is_step`，名单加仓不占台阶名额。lot 0 卖掉后已有台阶继续自己活。上证停开新仓，已持的名单加仓和台阶都可做。T+0 不加台阶。

买价 10：12.00 可加第 1 个台阶（自己的止损线 **9.60**）；14.00 可加第 2 个。

---

## 3. 未写明（六档与 T 窗不动）

| 项 | 实施默认 | 理由 |
|----|----------|------|
| 六档数字 | **不动** | 未提改止盈 |
| 评止盈日 | **T+2** | 现行 bands6 |
| giveback | **改为 ×0.80** | 与止损 20% 对齐；若仍留 ×0.90，T+2 起会先按 −10% 出，20% 止损几乎只在 T+1 生效 |
| 上证 / 峰差 | 停开新仓、已持可加；`PEAK_GAP_MIN=0` | 未提 |
| 名单窗现金 | `stock_pool/` 20251023–20260909 5 亿 | 未提 |

若 giveback 仍要 ×0.90，或止盈也改 T+4，写在批准句里。

---

## 4. 批准实施后的完整买卖点

```
止损 20%（T+1 起）
T+1 只评止损；T+2 起评止盈
T+2 起现价 ≤ 买价×0.80 → force_sell:giveback
(0,6%) 且涨幅 <3%     不评档位
(0,6%) 且涨幅 ≥3%     max(买价×1.01, 买价+50%×涨幅)
[6%,15%)              max(买价×1.04, 买价+60%×涨幅)
[15%,30%)             max(买价×1.10, 买价+60%×涨幅)
[30%,60%)             max(买价×1.20, 买价+70%×涨幅)
[60%,100%)            max(买价×1.50, 买价+80%×涨幅)
≥100%                 max(买价×1.80, 买价+80%×涨幅)
满 30 日且从未到 +6% → stale
已持出现在当日名单 → 加一个 100 万（输家也加）
相对第一笔成本每满 +20% → 再加一个独立 100 万（名单外也评）
新 lot 独立
上证十日线两日下方停开新仓、已持可加
每码 100 万整笔
```

---

## 5. 明确不改

- 策略 1–6 / 9 / 10，成交核 E-R1–E-R6
- `_v8_stop10_bands6` / `_stop10` / `_v8_t4_list_step20_ind_stale30`：不覆盖
- Mode B 利弗莫尔冻结本
- 未再一句「批准实施」之前：不改 Python、不跑 5 亿

---

## 6. 批准实施后改哪些文件

| 文件 | 做什么 |
|------|--------|
| `strategy8_rules.py` | `STOP_PCT=0.20`；`STALE_DAYS=30`；`GIVEBACK_MULT=0.80`；`may_add` 已持 `px>0`；`ADD_STEP=0.20` 并恢复 `step_add_due`；HELP |
| `csv_strategy_books.py` | version8 再挂 `step_add` |
| `csv_artifacts.py` / README | 摘要句与策略 8 段 |
| v8 单测 | 止损 20%、输家可加、台阶独立、僵持 30、T+2 giveback ×0.80 |

落地后跑分钟 5 亿，目录 **`_v8_bands6_addall_step20_stale30_stop20`**。

---

## 7. 单测向量（批准实施后要绿）

1. `stop_hits(8.01, 10)` False；`stop_hits(7.99, 10)` True。
2. `may_add([10], 9.90)` True；`may_add([10], 11.00)` True。
3. `step_add_due([lot0@10], 11.99)` False；`12.00` True；已有 1 个 `is_step` 时 13.99 False、14.00 True。
4. T+1 豁免止盈；T+2 起 `px=8.00` 记 giveback。
5. 满 29 日未武装不 stale；满 30 日记 stale。
6. 六档线与 `_v8_stop10_bands6` 相同。

---

## 8. 落地

2026-09-18 确认人「批准实施」后已改名单全加、独立 +20% 台阶、僵持 30、止损 20%、giveback ×0.80。分钟 5 亿退出码 0：`backtest_output/csv_minute_v8_20251023_20260909_v8_bands6_addall_step20_stale30_stop20/`。期末净值 **507,050,596.62 / +1.41%**。买入 3260、加仓 272（名单 + `add:step20` 52）、`skip_add_loser` 0、止损 223、回撤 2625、强制 374（stale 370 + giveback 4）。未覆盖 `_v8_stop10_bands6`（+1.51%）/ `_stop10`（+2.96%）/ `_v8_t4_list_step20_ind_stale30`（+1.20%）。
