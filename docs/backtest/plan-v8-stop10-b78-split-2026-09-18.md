# Plan：b78 五档内再拆线

> **日期**：2026-09-18
> **状态**：✅ **已落地并已跑分钟 5 亿**。目录 `_v8_stop10_b78_split`（509,980,047.85 / +2.00%）。不覆盖 `_stop10` / `_v8_3` / `_v8_livermore` / `_v8_3_hold5` / `_v8_3_hold5_stale8` / `_v8_stop10_t4` / `_v8_t4_stop30_add` / `_v8_t4_stop30_add20` / `_v8_t4_stop10_add20` / `_v8_t4_stop30_last12_keep20` / `_v8_stop10_b78`。
> **风险档**：L1+（只改策略 8 档内触发线；不改 1–6/9/10，不改 E-R1–E-R6 成交核）
> **基线书**：现行 stop10-b78，产物 `_v8_stop10_b78`（509,413,121.58 / +1.88%）
> **现行代码**：stop10-b78。落地时**只改档位线**，止损 / T+1 豁免 / giveback / 只加赢家 / 上证已持可加 / 僵持 20 不动。

---

## 0. 一句话

五档开闭不变。档 1 增加 0–3% 死区；档 1–4 在档内再按涨幅拆一条保底；档 5 只保留涨幅 70%、不再加 1.60 保底。

---

## 1. 已确认（2026-09-18，原话）

| # | 确认人原话 | 锁定 |
|---|------------|------|
| 1 | 「b78策略修改」 | 在现行 b78 上改档位线，不是另起 T4 / stop30 书。 |
| 2 | 「档 1：(0,6%) 涨幅＜3%不做判断 涨幅≥3% 保留涨幅30%且现价 ≥ 买价×1.01」 | 峰值涨幅 `<3%` 不评回撤。`≥3%` 且仍在 `(0,6%)`：线 = `max(买价×1.01, 买价+30%×涨幅)`。 |
| 3 | 「档 2：[6%,15%) 涨幅＜9% 保留涨幅30%且现价 ≥ 买价×1.02，涨幅≥9% 保留涨幅30%且现价 ≥ 买价×1.03」 | `<9%`：`max(×1.02, +30%×涨幅)`。`≥9%`：`max(×1.03, +30%×涨幅)`。 |
| 4 | 「档 3：[15%,50%] 涨幅＜30% 保留涨幅50%且现价 ≥ 买价×1.10，涨幅≥30% 保留涨幅50%且现价 ≥ 买价×1.15」 | `<30%`：`max(×1.10, +50%×涨幅)`。`≥30%`：`max(×1.15, +50%×涨幅)`。原 b78 档 3 是保留 60% + 单一 1.15。 |
| 5 | 「档 4：(50,100%] 涨幅＜70% 保留涨幅50%且现价≥买价×1.35，涨幅≥70% 保留涨幅50%且现价 ≥ 买价×1.5」 | `<70%`：`max(×1.35, +50%×涨幅)`。`≥70%`：`max(×1.50, +50%×涨幅)`。 |
| 6 | 「档 5：＞100%，保留涨幅70%」 | 线 = `买价+70%×涨幅`。不再用 b78 的 `max(×1.60, +60%×涨幅)`。 |

「保留涨幅 X% 且现价 ≥ 买价×M」一律译成触发线 `max(买价×M, 买价+X%×涨幅)`，`px≤线` 则 `trail:band:{1..5}`。档内拆线不另开 reason。

买价 10 时（相对现行 b78）：

| 峰值 | 档 | 现行 b78 线 | 新线 |
|------|----|-------------|------|
| 10.20（+2%） | 1 | 10.10 | **不评** |
| 10.40（+4%） | 1 | 10.12 | **10.12**（保底 1.01 与保留 30% 接近） |
| 10.80（+8%） | 2 | 10.20 | **10.24** |
| 11.00（+10%） | 2 | 10.20 | **10.30** |
| 12.00（+20%） | 3 | 11.50 | **11.00** |
| 14.00（+40%） | 3 | 12.40 | **12.00** |
| 16.00（+60%） | 4 | 13.50 | **13.50** |
| 18.00（+80%） | 4 | 14.00 | **15.00** |
| 22.00（+120%） | 5 | 17.20 | **18.40** |

档 1 死区会少刮薄利；档 2 / 档 4≥70% / 档 5 线更高（回撤更早下车）；档 3 线更低（多留一段）。

---

## 2. 未写明（实施时按此，要改请先说）

| 项 | 实施默认 | 理由 |
|----|----------|------|
| 五档开闭 | `(0,6%)` / `[6%,15%)` / `[15%,50%]` / `(50%,100%]` / `>100%` | 确认人沿用 b78 五档，只改档内 |
| 恰好 +3% / +9% / +30% / +70% | 走「≥」那一侧 | 原话写 ≥ |
| 恰好 +6% / +15% / +50% / +100% | 仍归上一档的右闭或下一档的左闭，与 b78 相同 | 未改开闭 |
| 档 5 保底 | 无额外乘数，只保留 70% | 原话没写 ×1.60 |
| 止损 / giveback / T+1 豁免 | 止损 10%；T+1 只评止损；T+2 起 `px≤买价×0.90` | 未提，沿 b78 |
| 僵持 | 满 20 日且峰值从未到 +6% | 未提。0–3% 死区不改变 stale 的 +6% 武装线 |
| 加仓 | 只加赢家；单码单日最多 2 笔 | 未提 |
| 上证十日线 | 停开新仓，已持仍可加 | 未提；引擎已接 `INDEX_BLOCKS_ADD=False` |
| 峰差 | `PEAK_GAP_MIN=0` | 未提 |
| 名单 / 窗 / 现金 | `stock_pool/`，20251023–20260909，5 亿 | 近几轮相同 |
| 跌停 / T+0 / 佣金 / E-R6 | 引擎原样 | 不改成交核 |

---

## 3. 批准实施后的完整买卖点

```
止损 10%（T+1 起）
T+1 只评止损；T+2 起评止盈
T+2 起现价 ≤ 买价×0.90 → force_sell:giveback
(0,6%) 且涨幅 <3%     不评档位回撤
(0,6%) 且涨幅 ≥3%     max(买价×1.01, 买价+30%×涨幅)
[6%,15%) 且涨幅 <9%   max(买价×1.02, 买价+30%×涨幅)
[6%,15%) 且涨幅 ≥9%   max(买价×1.03, 买价+30%×涨幅)
[15%,50%] 且涨幅 <30% max(买价×1.10, 买价+50%×涨幅)
[15%,50%] 且涨幅 ≥30% max(买价×1.15, 买价+50%×涨幅)
(50%,100%] 且涨幅 <70% max(买价×1.35, 买价+50%×涨幅)
(50%,100%] 且涨幅 ≥70% max(买价×1.50, 买价+50%×涨幅)
>100%                 买价+70%×涨幅
满 20 日且峰值从未到 +6% → stale
只加赢家；上证十日线两日下方停开新仓、已持可加
每码 100 万整笔
```

`px<cost` 不评档位回撤。止损在引擎里先于止盈。各 lot 独立。

---

## 4. 明确不改

- 策略 1–6 / 9 / 10，成交核 E-R1–E-R6
- 归档 `_stop10` / `_v8_3` / `_v8_livermore` / `_v8_3_hold5` / `_v8_3_hold5_stale8` / `_v8_stop10_t4` / `_v8_t4_stop30_add` / `_v8_t4_stop30_add20` / `_v8_t4_stop10_add20` / `_v8_t4_stop30_last12_keep20` / `_v8_stop10_b78`：不覆盖
- Mode B 利弗莫尔冻结本 `livermore_exit_rules.py`
- 未再一句「批准实施」之前：不改 Python、不跑 5 亿

---

## 5. 批准实施后改哪些文件

| 文件 | 做什么 |
|------|--------|
| `backtest/research/strategy8_rules.py` | `band_of` 仍 1–5；`trigger_line` 按上表拆；档 1 `<3%` 在 `take_profit_reason` 直接 None；HELP_LOCK / `record_strategy8_params` |
| `backtest/research/csv_artifacts.py` | summarize 写清死区与档内保底（`tp_min_days<4` 分支） |
| `docs/backtest/README.md` | 策略 8 段改本包，链本 plan |
| `tests/test_strategy8_rules.py` 及 v8 daily/minute | 按下节向量翻 |

落地后跑分钟 5 亿，目录 **`_v8_stop10_b78_split`**。不覆盖 `_v8_stop10_b78`。

---

## 6. 单测向量（批准实施后要绿）

买价 10，均 `n_days=2`（T+1 仍一律不评止盈）：

1. 峰值 10.20（+2%）、现价 10.00 → 不评。
2. 峰值 10.40（+4%）、现价触/不触 10.12 ±0.01 → `trail:band:1`。
3. 峰值 10.80（+8%）、线 10.24 → `trail:band:2`。
4. 峰值 11.00（+10%）、线 10.30 → `trail:band:2`。
5. 峰值 12.00（+20%）、线 11.00 → `trail:band:3`。
6. 峰值 14.00（+40%）、线 12.00 → `trail:band:3`。
7. 峰值 16.00（+60%）、线 13.50 → `trail:band:4`。
8. 峰值 18.00（+80%）、线 15.00 → `trail:band:4`。
9. 峰值 22.00（+120%）、线 18.40 → `trail:band:5`。
10. `n_days=1` 破任何新线 → 不止盈；现价 −10% → 止损。

---

## 7. 落地

2026-09-18 确认人回复「批准实施」后已按上表改档内拆线。分钟 5 亿退出码 0：`backtest_output/csv_minute_v8_20251023_20260909_v8_stop10_b78_split/`。期末净值 509,980,047.85 / +2.00%。买入 3198、加仓 307、止损 644、回撤 2382、强制 151。未覆盖 `_stop10` / `_v8_3` / `_v8_livermore` / `_v8_3_hold5` / `_v8_3_hold5_stale8` / `_v8_stop10_t4` / `_v8_t4_stop30_add` / `_v8_t4_stop30_add20` / `_v8_t4_stop10_add20` / `_v8_t4_stop30_last12_keep20` / `_v8_stop10_b78`。
