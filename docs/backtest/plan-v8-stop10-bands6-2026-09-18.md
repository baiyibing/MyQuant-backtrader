# Plan：归档 stop10 底盘 + 六档 max(保底, keep)

> **日期**：2026-09-18
> **状态**：✅ **已落地并已跑分钟 5 亿**。目录 `_v8_stop10_bands6`（507,573,114.90 / +1.51%）。未覆盖 `_stop10` / `_v8_t4_list_step20_ind_stale30`。
> **风险档**：L1+（只换策略 8 止盈档与摘要句；不改 1–6/9/10，不改成交核）
> **基线书**：归档 stop10，短记 [v8-stop-tp-opt-note-2026-09-16.md](v8-stop-tp-opt-note-2026-09-16.md)，产物 `_v2_stale20_sse_ma10_stop10`（514,788,283.89 / +2.96%）
> **现行代码**：t4-list-step20-ind-stale30（`_v8_t4_list_step20_ind_stale30`，+1.20%）。落地时**卖点整本换成下节锁表**，不是在 T+4 / 台阶包上打补丁。

---

## 0. 一句话

止损仍 10%。止盈改成确认人六档，每档都是 `max(买价×保底, 买价+keep×峰值涨幅)`。档 1 只在 3%–6% 触发。底盘回到归档 stop10：T+2 起评止盈、只加赢家、僵持 20 日、无 +20% 台阶。

---

## 1. 已确认（2026-09-18，原话）

| # | 确认人原话 | 锁定 |
|---|------------|------|
| 1 | 「止损10个点」 | `STOP_PCT=0.10`。T+1 起评。 |
| 2 | 「涨幅≥100% max（保底买价*1.8，买价+最高涨幅80%）」 | 峰值涨幅 `≥100%`：线 = `max(买价×1.80, 买价+80%×涨幅)`。`trail:band:6`。 |
| 3 | 「100%＞涨幅≥60% max（保底买价*1.5，买价+最高涨幅80%）」 | `60% ≤ 涨幅 < 100%`：`max(×1.50, keep80)`。`trail:band:5`。 |
| 4 | 「60%＞涨幅≥30% max（保底买价*1.2，买价+最高涨幅70%）」 | `30% ≤ 涨幅 < 60%`：`max(×1.20, keep70)`。`trail:band:4`。 |
| 5 | 「30%＞涨幅≥15% max（保底买价*1.1，买价+最高涨幅60%）」 | `15% ≤ 涨幅 < 30%`：`max(×1.10, keep60)`。`trail:band:3`。 |
| 6 | 「15%＞涨幅≥6% max（保底买价*1.04，买价+最高涨幅60%）」 | `6% ≤ 涨幅 < 15%`：`max(×1.04, keep60)`。`trail:band:2`。 |
| 7 | 「6%＞涨幅≥0% max（保底买价*1.01,买价+最高涨幅50%（3，6%）触发）」 | `0% < 涨幅 < 6%` 归档 1。**涨幅 <3% 不评**。`3% ≤ 涨幅 < 6%`：`max(×1.01, keep50)`。`trail:band:1`。 |

边界按原话的「≥」贴下沿、上沿归更高档：恰好 +6% / +15% / +30% / +60% / +100% 分别进档 2 / 3 / 4 / 5 / 6。恰好 +3% 计入档 1 触发。

「最高涨幅」= 峰值 − 买价（未平 lot 的 peak，high 刷新）。`px ≤ 线` 离场。

---

## 2. 未写明（按上一句归档 stop10，要改请先说）

上一句刚调归档 stop10，本包**不沿用**现行 T+4 / 名单台阶 / 僵持 30。

| 项 | 实施默认 | 理由 |
|----|----------|------|
| 评止盈日 | **T+2**（`TP_MIN_DAYS=2`） | 归档 stop10：T+1 只评止损 |
| giveback | T+2 起 `px≤买价×0.90` | 与止损 10% 对齐，几乎不再单独触发 |
| 加仓 | **只加赢家**：现价 ≥ **每一笔**未平成本 | 归档原文「低于任一成本不加」。关掉 `ADD_STEP` |
| 僵持 | 满 **20** 日且从未到 +6% | 归档；`never_armed` 仍按 +6% |
| 上证 / 峰差 | 停开新仓、已持可加；`PEAK_GAP_MIN=0` | 归档 |
| 名单窗现金 | `stock_pool/` 20251023–20260909 5 亿 | 未提 |

若要留现行「T+1～T+3 只评止损 / 名单再现即加 / +20% 台阶 / 僵持 30」，写在批准句里。

---

## 3. 买价 10 的线

| 峰值 | 涨幅 | 档 | 线 = max(保底, keep) |
|------|------|----|----------------------|
| 10.20 | +2% | 1 | **不评** |
| 10.40 | +4% | 1 | max(10.10, 10.20)=**10.20** |
| 10.60 | +6% | 2 | max(10.40, 10.36)=**10.40** |
| 11.40 | +14% | 2 | max(10.40, 10.84)=**10.84** |
| 11.50 | +15% | 3 | max(11.00, 10.90)=**11.00** |
| 12.00 | +20% | 3 | max(11.00, 11.20)=**11.20** |
| 13.00 | +30% | 4 | max(12.00, 12.10)=**12.10** |
| 16.00 | +60% | 5 | max(15.00, 14.80)=**15.00** |
| 20.00 | +100% | 6 | max(18.00, 18.00)=**18.00** |
| 22.00 | +120% | 6 | max(18.00, 19.60)=**19.60** |

对照归档 stop10：档 2 从只 ×1.02（+14% 线 10.20）改成 max(×1.04, keep60)（+14% 线 **10.84**），低档锁得更紧；档 3 从 `max(×1.15, keep60)` 改成 `max(×1.10, keep60)`，+20% 线从 11.50 降到 11.20。

---

## 4. 批准实施后的完整买卖点

```
止损 10%（T+1 起）
T+1 只评止损；T+2 起评止盈
T+2 起现价 ≤ 买价×0.90 → force_sell:giveback
(0,6%) 且涨幅 <3%     不评档位
(0,6%) 且涨幅 ≥3%     max(买价×1.01, 买价+50%×涨幅)
[6%,15%)              max(买价×1.04, 买价+60%×涨幅)
[15%,30%)             max(买价×1.10, 买价+60%×涨幅)
[30%,60%)             max(买价×1.20, 买价+70%×涨幅)
[60%,100%)            max(买价×1.50, 买价+80%×涨幅)
≥100%                 max(买价×1.80, 买价+80%×涨幅)
满 20 日且从未到 +6% → stale
已持出现在当日名单、且现价 ≥ 每一笔未平成本 → 加一个 100 万
新 lot 独立；无 +20% 台阶
上证十日线两日下方停开新仓、已持可加（仍过赢家门）
名单外不加仓
每码 100 万整笔
```

---

## 5. 明确不改

- 策略 1–6 / 9 / 10，成交核 E-R1–E-R6
- 归档 `_stop10` / `_v8_t4_list_step20_ind_stale30` / 今天其余包：不覆盖
- Mode B 利弗莫尔冻结本
- 未再一句「批准实施」之前：不改 Python、不跑 5 亿

---

## 6. 批准实施后改哪些文件

| 文件 | 做什么 |
|------|--------|
| `backtest/research/strategy8_rules.py` | 六档 `band_of` / `trigger_line`；`KEEP` / 保底常数；`TP_MIN_DAYS=2`；`STALE_DAYS=20`；`ADD_STEP=0`；`may_add` 只加赢家；关掉 `step_add_due` 有效路径；HELP / `record_strategy8_params` |
| `backtest/research/csv_strategy_books.py` | version8 去掉 `step_add` 钩子（本包无台阶） |
| `backtest/research/csv_artifacts.py` | summarize 写六档保底/keep |
| `docs/backtest/README.md` | 策略 8 段改本包，链本 plan |
| v8 单测 + `test_exdiv_refprice_engines.py` T5 | 按下节翻。T5 现线 5.75 会变成 5.60 |

落地后跑分钟 5 亿，目录 **`_v8_stop10_bands6`**。不覆盖 `_stop10` / `_v8_t4_list_step20_ind_stale30`。

---

## 7. 单测向量（批准实施后要绿）

1. `STOP_PCT=0.10`；`stop_hits(9.01, 10)` False；`stop_hits(8.99, 10)` True。
2. 档 1 死区：峰 10.20 不评；峰 10.40 线 10.20。
3. 档 2 峰 10.60 线 10.40；峰 11.40 线 10.84。
4. 档 3 峰 11.50 线 11.00；峰 12.00 线 11.20。
5. 档 4 峰 13.00 线 12.10。
6. 档 5 峰 16.00 线 15.00。
7. 档 6 峰 20.00 线 18.00；峰 22.00 线 19.60。
8. `may_add([10], 9.90)` False；`may_add([10], 10.00)` True。
9. T+1 仍豁免止盈与 giveback。

---

## 8. 落地

2026-09-18 确认人「批准实施」后已整本换成上表。分钟 5 亿退出码 0：`backtest_output/csv_minute_v8_20251023_20260909_v8_stop10_bands6/`。期末净值 **507,573,114.90 / +1.51%**。买入 3177、加仓 176、`skip_add_loser` 15、止损 638、回撤 2373、强制 148、`skip_cash` 0。未覆盖 `_stop10`（仍 +2.96%）/ `_v8_t4_list_step20_ind_stale30`。
