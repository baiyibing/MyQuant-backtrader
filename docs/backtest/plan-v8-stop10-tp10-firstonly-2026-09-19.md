# Plan：stop10-tp10-reserve 优化为只做首笔盒子

> **日期**：2026-09-19
> **状态**：✅ **已落地并已跑分钟 5 亿**。目录 `_v8_stop10_tp10_firstonly`（512,144,050.98 / +2.43%）。低于基线 reserve 的 +3.43%。未覆盖 `_v8_stop10_tp10_reserve`。
> **风险档**：L2（关加仓 / 关涨停保留；不改成交核）
> **基线书**：stop10-tp10-reserve，产物 `_v8_stop10_tp10_reserve`（517,134,408.63 / +3.43%）
> **对照**：归档 stop10 +2.96%；addall +1.41%

---

## 0. 一句话

±10% 盒子留着。名单加和 +20% 台阶关掉。涨停不再保留，按 +10% 止盈出。

---

## 1. 基线拆账里要改的三件事

| 现象 | 数字 | 锁定 |
|------|------|------|
| 首笔赚、名单加几乎不赚却占满仓 | 首笔 +1,579 万；名单加 738 笔只 +222 万；占用峰 5.40 亿；`skip_cash` 33 | `ALLOW_ADD=False`。已持再现记 `skip_held` |
| +20% 台阶本窗 0 笔 | 止盈在 +10% 先平，开板也先卖，台阶到不了 | `ADD_STEP=0`，不挂 `step_add` |
| 10% 板涨停保留把现成止盈换成开板 | 开板 35 笔全是 10% 板，30 笔低于 +10%；20% 板 0 笔开板 | `RESERVE_LIMIT_UP=False`。涨停收盘≥买价×1.10 按 `profit_take:target` 出 |

未改：止损 10%、止盈 10%、T+1 起评、上证十日线停开新仓、每码 100 万、`stock_pool/` 5 亿窗。

---

## 2. 完整买卖点

```
止损 10%（T+1 起）
T+1 起现价 ≥ 买价×1.10 → profit_take:target
涨停按止盈出，不保留至开板
已持再现名单 → skip_held，不加
无 +20% 台阶
上证十日线两日下方停开新仓
每码 100 万整笔，只做第一笔
无六档、无 giveback、无僵持
```

---

## 3. 明确不改

- 策略 1–6 / 9 / 10，成交核 E-R1–E-R6
- Mode B 利弗莫尔冻结本
- `_v8_stop10_tp10_reserve` / `_v8_bands6_addall_step20_stale30_stop20` / `_v8_stop10_bands6` / `_stop10`：不覆盖

---

## 4. 改哪些文件

| 文件 | 做什么 |
|------|--------|
| `strategy8_rules.py` | `ALLOW_ADD=False`；`ADD_STEP=0`；`RESERVE_LIMIT_UP=False` |
| `csv_strategy_books.py` | version8 不挂 `step_add`；`reserve_limit_up` 跟常量 |
| `csv_artifacts.py` / README | 摘要改为止损 10% / 止盈 10% / 只做首笔 |
| v8 单测 | 已持 skip_held；涨停按止盈；无开板 / 无台阶 |

落地目录 **`_v8_stop10_tp10_firstonly`**。

---

## 5. 落地

2026-09-19 确认人「优化并执行」后已换成只做首笔盒子。分钟 5 亿退出码 0：`backtest_output/csv_minute_v8_20251023_20260909_v8_stop10_tp10_firstonly/`。期末净值 **512,144,050.98 / +2.43%**，最大回撤 −4.24%。买入 2632、加仓 0、`skip_held` 740、止损 1241、止盈 1356、开板 0、`skip_cash` 0。

对照基线 `_v8_stop10_tp10_reserve`（仍 +3.43% / 517,134,408.63）：本包低约 499 万。拆账里名单加 +222 万和开板 +287 万在关掉之后没有被更多首笔补回来。回撤从 −5.83% 收到 −4.24%，占用不再顶满。未覆盖 reserve / addall / bands6 / stop10。
