# Plan：策略 6 止盈时钟改为 T+1 起评

> **日期**：2026-09-18
> **状态**：✅ **已落地并已跑分钟 5 亿**。目录 `_v6_list_add_tp_t1_dd70_50`（499,328,032.36 / −0.13%）。不覆盖 `_v6_list_add_t1_dd70_50`。
> **风险档**：L1（只改 `TP_MIN_DAYS`；加仓 / 止损 / 两档回撤数字不动）
> **现行代码**：名单加仓；止损 6%；`TP_MIN_DAYS=1`（T+1 起评止盈）；&lt;6% 回撤 70%；≥6% 回撤 50%
> **对照**：`_v6_list_add_t1_dd70_50`（499,288,780.41 / −0.14%）。不覆盖该目录，也不覆盖策略 8 归档。

---

## 0. 一句话

止盈从 **T+1 日起评**（`n_days>=1`）。T+0 仍不能卖。止损、两档回撤、名单加仓、日额度均分全部不动。

---

## 1. 已确认（2026-09-18，原话）

| # | 确认人原话 | 锁定 |
|---|------------|------|
| 1 | 「strategy6_rules修改止盈时钟T+1日起评」 | `TP_MIN_DAYS=1`。`n_days<1` 不评止盈。T+1（`n_days=1`）起评 `lt6` / `ge6`。 |

`n_days`：买入日 = 0，下一交易日 = 1 = T+1。引擎本就 `n_days<1` 不可卖。

上一包「T+1 日不止盈」作废，只改这一条。

---

## 2. 改什么 / 不改什么

| 项 | 现行 | 本包 |
|----|------|------|
| 止盈时钟 | T+1 不评，T+2 起 | **T+1 起评** |
| 止损 6% | T+1 起 | 不动 |
| &lt;6% 回撤 70% / ≥6% 回撤 50% | 线公式不动 | 不动 |
| 名单加仓 | 输家也加 | 不动 |
| 峰差 15 分钟 / 日额度 | 不动 | 不动 |
| 策略 10 | 旧 +1% 锚、已持跳过 | **不动** |

买价 10：T+1 峰值 10.50、现价 10.15 → 现在记 `trail:band:lt6`（上一包这里是 None）。

上一包 T+1 已平 338 笔全是止损。本包同一天若先涨后回撤，会多出一批 T+1 止盈；止损仍优先。

---

## 3. 明确不改

- 策略 1–5 / 7 / 8 / 9 / 10
- 涨跌停 / 跌停顺延 / T+0 不可卖 / 佣金 / E-R6 / 涨停 T+1 09:45 追买
- 归档 `_v6_list_add_t1_dd70_50` 与全部策略 8 归档：不覆盖
- 未再一句「批准实施」之前：不改 Python、不跑回测

---

## 4. 批准实施后改哪些文件

| 文件 | 做什么 |
|------|--------|
| `strategy6_rules.py` | `TP_MIN_DAYS=1`；模块说明 / HELP |
| `csv_artifacts.py` | `v6_dd_bands` 文案：`T+1起评止盈` |
| `docs/backtest/README.md` | 策略 6 一段改时钟 |
| `tests/test_strategy6_rules.py` | `n_days=1` 可出 lt6/ge6；`n_days=0` 仍 None |
| `tests/test_csv_daily_backtest.py` | T+1 收盘已触线则次日开盘卖（lt6/ge6 夹具改期望日） |

落地后默认不跑 5 亿。若要同窗 `stock_pool/` 20251023–20260909、`--cash-total 500000000`，写在同一句。目录拟 `_v6_list_add_tp_t1_dd70_50`。

---

## 5. 单测向量（批准实施后要绿）

1. `n_days=0`：峰值 10.50、现价 10.15 → None。
2. `n_days=1`：同价 → `trail:band:lt6`；峰值 10.60、现价 10.30 → `trail:band:ge6`。
3. 止损 9.40 仍 T+1 可出，且优先于止盈。
4. 日线：T+1 收盘 ≤ 线 → 次日开盘成交（不再等到 T+2 收盘）。
5. version10 `allow_add is False`；旧 +1% 锚向量仍绿。

---

## 6. 落地

2026-09-18 确认人回复「批准实施」后已改：`TP_MIN_DAYS=1`，T+1 起评 lt6/ge6。针对性 pytest 149 passed。随后「跑程序」分钟 5 亿退出码 0：`backtest_output/csv_minute_v6_20251023_20260909_v6_list_add_tp_t1_dd70_50/`。期末净值 **499,328,032.36 / −0.13%**。买入 4468、加仓 37、`skip_held` 0、止损 946、回撤 3515。未覆盖 `_v6_list_add_t1_dd70_50`（499,288,780.41 / −0.14%）。
