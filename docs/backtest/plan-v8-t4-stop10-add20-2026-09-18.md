# Plan：T4-stop30-add20 止损改 10%

> **日期**：2026-09-18
> **状态**：✅ **已落地并已跑分钟 5 亿**。目录 `_v8_t4_stop10_add20`（506,673,601.77 / +1.33%）。不覆盖 `_stop10` / `_v8_3` / `_v8_livermore` / `_v8_3_hold5` / `_v8_3_hold5_stale8` / `_v8_stop10_t4` / `_v8_t4_stop30_add` / `_v8_t4_stop30_add20`。
> **风险档**：L1+（只换策略 8 止损；T+4 / 档位 / 20% 加仓门不动；不改 1–6/9/10，不改 E-R1–E-R6 成交核）
> **基线书**：现行 T4-stop30-add20，计划 [plan-v8-t4-stop30-add20-2026-09-18.md](plan-v8-t4-stop30-add20-2026-09-18.md)，产物 `_v8_t4_stop30_add20`（+0.58%）

---

## 0. 一句话

确认人原话「止损改为10%」：`STOP_PCT=0.10`。T+1 起评。无 giveback。其余卖点与加仓门不动。

---

## 1. 完整买卖点

```
止损 10%（T+1 起；无 giveback）
T+4 起评止盈；档位与 T4-stop30-add20 相同
满 20 日且峰值从未到 +6% → stale
已持加仓：现价相对最早未平成本每满 20% 一档，且当日名单有该码
        一次一个基数（100 万）；各 lot 独立评卖
上证十日线两日下方：当日新开与加仓都不做
```

---

## 2. 明确不改

- T+4、档位数字、僵持 20、峰差 15、20% 加仓阶梯
- 策略 1–6 / 9 / 10，成交核 E-R1–E-R6
- 归档 `_stop10` / `_v8_3` / `_v8_livermore` / `_v8_3_hold5` / `_v8_3_hold5_stale8` / `_v8_stop10_t4` / `_v8_t4_stop30_add` / `_v8_t4_stop30_add20`
- Mode B 利弗莫尔冻结本 `livermore_exit_rules.py`

---

## 3. 单测

1. `n_days=1`、现价 −10% → 止损；−9.89% 不止损。
2. −20% 现在止损（上一本 30% 时不止）。
3. T+4 档位线与上一本相同。
4. 创业板跳空：D1/D2 贴跌停顺延两次，D3 `gap_open`。

---

## 4. 落地

2026-09-18 确认人「止损改为10%」后已改 `STOP_PCT=0.10`。分钟 5 亿退出码 0：`backtest_output/csv_minute_v8_20251023_20260909_v8_t4_stop10_add20/`。期末净值 506,673,601.77 / +1.33%。买入 2821、加仓 1、止损 709、回撤 1964、强制 126。未覆盖 `_stop10` / `_v8_3` / `_v8_livermore` / `_v8_3_hold5` / `_v8_3_hold5_stale8` / `_v8_stop10_t4` / `_v8_t4_stop30_add` / `_v8_t4_stop30_add20`。
