[build-system]
requires = ["maturin>=1.4,<2"]
build-backend = "maturin"

[project]
name = "turnover_resist"
requires-python = ">=3.10"
dynamic = ["version"]

[tool.maturin]
manifest-path = "Cargo.toml"
module-name = "turnover_resist.turnover_resist"
python-source = "python"
